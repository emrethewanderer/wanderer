---
name: ic-meclis-suretler
description: "İç Meclis / Suretler — Hasımlar'ın yerine geçen parça-temelli sistem; faz planı ve motor kararları"
metadata: 
  node_type: memory
  type: project
  originSessionId: bcce1b46-6327-467f-a532-c8ee2415dc81
---

"Hasımlar" bölümü tamamen yeniden tasarlanıyor → **İç Meclis · Suretler** (IFS/parça psikolojisi). Felsefe: kullanıcı tek bir kişi değil, bir kadrodur; her parça bir "Suret" (erteleyen, onay-dilenci...). "Arkadaki Sen" = meclisin reisi. Çekirdek ton: **Bütünleştirme/Reislik** (öldürmek değil tanı→adlandır→dönüştür); savaş/can/saldırı dili atıldı.

**Motor:** Ölü `invisible_face` zinciri (eski gölge şeması shadow_title/blind_spots/defense_mechanism artık kullanılmıyordu) yeniden canlandırıldı; tek AI geçişi hem "Hayattaki Sen" haritasını hem `suretler[]` dizisini üretir. `generateInvisibleFaceProfile` ([13-extras.js]) export edildi, maxTokens 450→750. Prompt: 16b-i18n-prompt-dict-core.js (TR + EN çevrildi, diğer 11 dil bekliyor).

**Faz 1 (TAMAMLANDI, 2026-06-01):** Suret üretimi + Meclis Salonu galerisi + Adlandırma seremonisi. Yeni modül [js/parts/10p-w2-meclis.js], DB `migrations/004_suretler.sql` (suretler tablosu, owner RLS, sadece adlanan suretler yazılır; taslaklar SafeStorage'da aylık cache). Route `hasimlar` → `window.loadMeclisView()` (03-auth-shell.js). Adlandırma → `awardElmas(15)`. Test: tests/10p-meclis.test.js (7 test). Eski `loadHasimlarView` (Sefer/boss sistemi) artık route'tan çağrılmıyor ama hâlâ export/expose — Faz 2'de Meydan Okuma olarak bağlanacak.

**Faz 2 (TAMAMLANDI, 2026-06-01):** Meydan Okuma motoru Meclis İÇİNDE sıfırdan kuruldu — eski `challenge_progress`/boss-Sefer plumbing'ine BAĞLANMADI (kullanıcı "komple değiştir" dedi). Durum `suretler` satırında (migration 005: son_yuzlesme, yuzlesme_sayisi, sefer_gun, sefer_son_muhur, sefer_baslangic). İki tip: **Yüzleşme** (günlük, AI'sız, bağ +6 / Elmas +5) ve **Sefer** (21 gün, modül içi TR/EN-hardcoded jenerik `SURET_SEFER_TASKS` şablonu {{ad}}/{{ayna}} ile kişiselleşir; gün mühürle bağ +5/Elmas +3, tamamlama Elmas +25). Bağ 0→100; 100'de "bütünleşmeye hazır". Fonksiyonlar: meclisYuzlesme/meclisStartSefer/meclisSealSeferDay. Bağ metresi kartta+modalda. Faz 2 hâlâ Diyalog/Kanıt tiplerini İÇERMEZ (Faz 3+).
**Faz 3 (TAMAMLANDI, 2026-06-01):** `hal` artık sezilen|adlandi|**butunlesti**. Migration 006 (diyalog_sayisi, son_diyalog, butunlesti_at). **Diyalog**: yeni `prompt.meclis_dialog` (TR+EN) ile Suret 1. tekil sesinde konuşur (callLLM skipPersona:true, ephemeral S._meclisDialog); günlük ilk diyalog bağ +4/Elmas +2 (meclisDialogReward). **Bütünleşme**: bag=100'de meclisButunles → hal=butunlesti, Elmas +50, bespoke dönüşüm töreni (_butunlesmeCeremony, ad→ayna + güçlenen Temeller/kokler) + opsiyonel "Dönüşüm Mührü" (showGraduation reuse, meclisDownloadMuhur). Kart altın foile döner (is-butunlesti). Stats BÜTÜNLEŞTİ = hal butunlesti. Fonksiyonlar: meclisOpenDialog/DialogSend/CloseDialog/DialogReward/Butunles/DownloadMuhur. 14 test (10p), 335 toplam yeşil.

**Faz 4 (TAMAMLANDI, 2026-06-01):** (1) **Bütünlük Skoru + Meclis Salonu başlığı** — computeButunluk (yalnızca tanınan yüzlerin _suretProgress ortalaması; butunlesti=100), gelişen salon metni (☉ reis/divan, %skor, müttefik sayısı, hall_0→hall_100 evrilen alt başlık). (2) **Kanıt** = 4. meydan okuma tipi (🜄): migration 007 kanitlar JSONB + son_kanit; meclisOpenKanit/meclisSaveKanit, günlük ilk kanıt bağ +5/Elmas +4, gerçek-hayat çıpası metni diziye eklenir. (3) Sağlamlık: _elmasSafe() guard. 18 test (10p), 339 toplam yeşil.

**Faz 5 (TAMAMLANDI, 2026-06-01):** **Derinlik Aynası** — döngü kapanışı. Migration 008 `meclis_derinlik` (user_id PK, baseline/latest JSONB+at, owner RLS). `generateInvisibleFaceProfile(force=false)` — force aylık cache'i atlar. loadMeclisView ilk profilde baseline'ı yakalar (ensureBaseline). Meclis Salonu başlığında "Derinlik Aynası" linki → meclisOpenDerinlik paneli (4 Derinlik: Başlangıç→Şimdi, ok yönleri, "X derinlik yükseldi"). meclisRemeasure force profil çeker, latest kaydeder, Elmas +20. Gating: canRemeasure = baz var + (21 gün VEYA bir suret bütünleşmiş). Pure helper'lar test edildi: parseDepthLevel (Zayıf/Orta/Güçlü→0/1/2, TR+EN), computeDepthDeltas (up/down/flat). 23 test (10p), 344 toplam yeşil.

**Migrationlar Supabase'e elle uygulanmalı:** 004 (suretler), 005 (sefer/yüzleşme), 006 (diyalog/bütünleşme), 007 (kanıt), 008 (meclis_derinlik). Uygulanmazsa local çalışır ama kalıcılık yok.
**Faz 6 (TAMAMLANDI, 2026-06-02):** Kitaplar pypdf ile baştan sona okundu (Wanderer İlişki Felsefesi 59s + Zihniyet Devrimi 513s). (1) **Epik Meclis Salonu** — _meclisHallHTML artık Divan sahnesi: taht (☉ = Arkadaki Sen/reis) + conic-gradient bütünlük halkası + oturan suret madalyonları (dönüşmüş=altın merkez/büyük, adlandı=altın çerçeve, sezilen=sönük/dashed/kenar). (2) **Kitap-uyumu terim düzeltmesi:** kullanıcı-yüzlü "Bütünleşme/Bütünleşti"→"Dönüşüm/Dönüştür/Dönüştü" (KİTAPTA bütünleşme=Hayattaki Sen ile kaynaşıp Asıl Sen sanmak=sorun, İlişki Felsefesi s14). İç hal değeri 'butunlesti' + fonksiyon adları korundu (sadece i18n + stat etiketi).

**KİTAP DAYANAKLARI (doğrulandı):** "Bütün bir insan olsan da farklı kişiler barındırdığını görebilirsin" (İlişki s21) + "her insan bir zihniyettir... sen de farklı zamanlarda farklı bir insan" (Zihniyet s25) → suretler. "Sevilmeyecek... kişi taraflarımızı analiz edip SİLİP yenilerini koyalım" (İlişki s56/4). Reis=Arkadaki Sen=Asıl Sen (İlişki s14) → tam uyumlu. Yöntem: Kendine-Sor + Hayal Gücü (1.tekil) + Olumlama/ses + davranış (ÇALIŞMA KAĞIDI). **KRİTİK (Zihniyet Böl.98): "istemediğin kişiyi BİL AMA ODAKLANMA"** → mevcut İç Meclis gölgelere fazla odaklanıyor; vurgu olmak istediğin kişiye (ayna) kaymalı. Divan'da down-payment yapıldı (altın müttefikler merkezde, sezilen sönük kenarda) ama tam rebalance kullanıcı onayı bekliyor.

**Faz 7 (TAMAMLANDI, 2026-06-02):** **"Bil ama odaklanma" rebalansı** (Zihniyet Böl.98) — DB/migration YOK, saf 10p sunum+şablon. (1) Suret kartı (adlandi+butunlesti hal'leri) artık AYNA öncelikli: kart açılınca `_aynaFocusHTML` = altın "OLMAK İSTEDİĞİN" kahraman bloğu + "{{ad}} belirdiğinde — tanı, sonra buraya dön" pivotu; gölge anatomisi (ses+niyet/korku/örüntü/kör nokta) `_shadowAnatomyHTML` ile katlanmış `<details class="meclis-tani">` ("Gölgeyi tanı (ama orada durma)") içine taşındı — görünür ama odak değil. **sezilen** hal'i değişmedi (tanıma anı tam reveal ister; `voice`+`insights` orada kalır). (2) **Sefer üçlüsü**: her aktif sefer gününe kitabın Sor→Hayal et→Olumla motoru (`SEFER_TRIAD` TR/EN-hardcoded, `_seferTriadHTML`) gömüldü — hepsi {{ayna}}'ya yönelir (gölgeye değil); günlük görev=tanı, üçlü=aynaya geç. `_aynaName(s)` yardımcısı (ayna||"olmak istediğin kişi"); _seferTaskFor de bunu kullanır. Build temiz (119 modül), bundle doğrulandı (tüm yeni dize/CSS/etiketler dist'te). NOT: interaktif kart doğrulaması auth+DB'de adlanmış suret gerektirir; bundle-presence ile doğrulandı.

**Dil sızıntısı düzeltmesi (2026-06-26):** "Türkçe kullanılırken İç Meclis İngilizce" şikayeti. Tanı: statik 10p/13i UI'si + sözlük (15b) + promptlar TAMAMEN dil-duyarlı/Türkçe; İngilizce yalnızca LLM-üretimli suret içeriğinden (unvan/ad/ses/niyet/ayna + derinlik haritası) gelir. **Kök neden:** `generateInvisibleFaceProfile` ([13-extras.js]) aylık cache anahtarı dil-namespace'siz (`etw_invisible_face_<uid>_<YYYY-MM>`) → bir kez İngilizce üretilince (örn. test sırasında dil EN'ken) Türkçe'ye dönülse de aynı ay aynı cache döner. Ayrıca `skipPersona:true` + Türkçe'de `getLangInstruction()` boş → üretim çağrısında dil kilidi yoktu. **Çözüm:** (1) cache anahtarına `_${S._currentLang}` eklendi (dil değişince yeni dilde regenerate, eski İngilizce cache eşleşmez → kendi kendini iyileştirir); (2) `prompt.invisible_face` TR+EN'e sert "DİL KİLİDİ / LANGUAGE LOCK" satırı; (3) `prompt.meclis_dialog` dil satırı güçlendirildi (tr fallback-güvenli kaldı). NOT: DB'ye adlanmış (hal=adlandi/butunlesti) suretler İngilizce kalır (kullanıcı o dilde adlandırdı) — gerekirse "Bu ben değilim"→yeniden tanı. Build 194 modül temiz, 23/23 10p test yeşil, konsol temiz. Runtime LLM-dil doğrulaması yapılamadı (gerçek geçmiş+API+nondeterministik).

**AÇIK RİSK:** 16b-i18n-prompt-dict-ext.js'teki `prompt.invisible_face` (de/fr/es/pt/it/nl/ru/ar/zh/ja/ko) hâlâ ESKİ shadow şeması (shadow_title/blind_spots/defense_mechanism — `suretler[]` YOK) → çekirdek-dışı dillerde İç Meclis suretleri boş/bozuk. Çekirdek-dışı dil ekibi gelince bu 11 prompt yeni suret şemasına çevrilmeli.

**Olası Faz 8:** orphan Engeller (6 perde/6 zehir/7 tuzak — Zihniyet Böl.92/93/139) entegrasyonu; diyalog kalıcılığı; 11 dil çevirisi (üstteki açık risk dahil); Sor/Hayal/Olumla üçlüsünün güne göre çeşitlenmesi (şu an her gün sabit).

## İç Meclis 2.0 — "Gölgeyi tanı, kartı çevir" (2026-07-12, Fable 5→Sonnet 5, plan `.claude/plans/dazzling-baking-willow.md`)

**Faz 8 gerçekleşti** — 10p-w2-meclis.js baştan yazıldı (66KB/1224 satır → 82KB/1605 satır), TÜM eski `window.meclis*`/`loadMeclisView`/vb. imzalar KORUNDU (yeni gövdeler eski adlara oturdu). Merkez kavram: **Suret = ters çevrilmiş kart** — gölge yüz (lapis, şu anki) ↔ altın yüz (ayna, OİK'ten). Divan artık iki kutup arasında: **Olduğun Kişi** (Benlik Kartı 2.0, `benlikGetContext/benlikCardName`) ↔ **Olmak İstediğin Kişi** (`oikGetDesired`).

**K1 Kart kimliği:** suretler artık 12c `ikvCardFace/ikvCardBack/ikvComposeBackdrop/ikvRing` ile basılıyor (299 bespoke `.meclis-card`/seat CSS'i büyük ölçüde ikv primitiflerine devredildi); sahne `window.kumEnsureSpec?.()` (12d, TDZ-güvenli) ile senkron heuristik + arka planda LLM rafine. `_suretToCard(s, 'golge'|'altin')` köprü fonksiyonu; kok→virtue eşlemesi (`oz_guven→ozguven` vb., eşleşmeyen `yansima`'ya düşer).

**K2 Huzura Çıkış:** `openSuretCard` sil baştan — tam-ekran `ikvComposeBackdrop` + nested-flip (outer=giriş animasyonu, inner=`transform-style:preserve-3d` rotateY flip; GOTCHA'ya göre doğru scope'landı, gerçek tarayıcıda doğrulandı). `meclisCloseDetail/meclisCloseButunlesCeremony/meclisCancelAdlandirma` yeni exported kapatıcılar (eski inline `this.closest('.overlay').remove()` yerine — instrumentation için).

**K3 OİK köprüsü:** `_aynaName(s)` artık `s.ayna` yoksa `window.oikGetDesired?.()?.name`'e düşer. YENİ `meclisSealToOik(slug)` — dönüşen suretin kazanımı onayla `window.oikAddMadde(kategori,text,src)` (10D'ye YENİ eklenen public yardımcı, `_getActiveCard`+`_addEntry`+`oikSave` zincirini reuse eder) ile OİK kartına işlenir; `suretler.oik_madde_id` izi idempotens sağlar. `generateInvisibleFaceProfile` promptuna `{{oikContext}}` eklendi (13-extras.js) — ayna alanı OİK'e hizalanır.

**K4 Kitap motoru:** Yüzleşme→**Fark Ediş** (fonksiyon adı `meclisYuzlesme` AYNEN); Sefer→**Yol**, 21 gün artık 3 perdeli: gün 0-6 TANI, 7-13 KÖK (suretin kendi niyet/korku/zırhından dinamik Sor/Gör/İndir üçlüsü, LLM'siz), 14-20 GEÇİŞ (eski Sor/Hayal et/Olumla). `seferPerde(gun)` export edildi (test edilebilir). Diyalog artık `diyaloglar` JSONB'ye deterministik özet düşürüyor (YENİ LLM çağrısı EKLENMEDİ — mevcut turun kısaltması).

**K5 Engel köprüsü:** her surete `resolveEngelId(texts,seed)` (LLM'siz, 10h ENGELLER üzerinde `.includes()` tarama + tohumlu düşüş — ASLA boş dönmez) ile bir engel_id atanıyor; detayda panzehir + "Engel Atlası →" linki. 10m-w2-engeller.js'e ÇİFT YÖNLÜ köprü: `_meclisSuretlerHTML(obId)` (S._suretler'i DOĞRUDAN okur, import gerekmez) + YENİ `engJumpToSuret(slug)` (switchView('meclis')→openSuretCard).

**K6 Çok kaynaklı seziş — YENİ analiz motoru İCAT EDİLMEDİ:** `ensureSuretDrafts` artık öncelik zinciri: **Kaynak A** `window.ypGetFullState()` (09e Yaşayan Portre) kor_noktalar+celiskiler → LLM'siz taslak (`kaynak:'yp'`) → **Kaynak B** `window.ypGetHipotezler()` (09g Ayna Protokolü) `durum==='dogrulandi'` olanlar mevcut taslağı `ap_confirmed` işaretler YA DA kendi taslağını açar (**hal hâlâ 'sezilen'** — Meclis'in kendi Tanı&Adlandır töreni ATLANMAZ, sadece güçlü aday rozeti eklenir) → **Kaynak C** (yalnız A+B boşsa) legacy aylık `generateInvisibleFaceProfile` → **Kaynak D** elle: YENİ `meclisOpenElleSezis/meclisSaveElleSezis` ("Bir yüz seziyorum" girişi, `prompt.meclis.sense` YENİ TR+EN, LLM yoksa tarif doğrudan unvan olur). Eski ayrı "Örüntü Motoru köprüsü" fikri TERK edildi — 09e zaten `omGetTopPatterns`'i günlük sentezine katıyor.

**Enstrümantasyon:** yeni törenler (`meclis-detay`, `meclis-adlandirma`, `meclis-donusum`, `meclis-elle-sezis`) `window.wtOverlayOpen/Close(name)` çağırıyor (00f Gözlemevi, 09h'nin belirlediği güncel standart).

**Migration 035 ELLE (Supabase'e uygulanmalı):** `suretler` tablosuna `sahne JSONB · engel_id TEXT · diyaloglar JSONB · kaynak TEXT · oik_madde_id TEXT`. Migration'sız da çalışır — `_suretUpdate/_suretUpsert` 42703 (undefined_column) yakalayıp yeni alanları atıp yeniden dener (031/10D kalıbı, `_meclisEvrimColsOk` bayrağı).

**Test + doğrulama:** tests/10p-meclis.test.js 23→38 test (15 yeni: resolveEngelId/seferPerde/_aynaName + yp/ap seziş kaynak zinciri + meclisSealToOik idempotens); TAM vitest paketi 635/635 yeşil (39 dosya); build 213 modül temiz. **Gerçek tarayıcıda uçtan uca doğrulandı** — `meclis-test.html` (repo kökü, GEÇİCİ harness, kisilerim-test.html kalıbı + `Object.assign(window, meclis)` ile modülün kendi onclick="" dizeleri çözülür): Divan+iki kutup+mini kartlar, sezilen kart-sırtı, adlandı flip+perde-üçlü+panzehir, bütünleşti flip+OİK-mühür-idempotens, elle-seziş modalı, adlandırma seremonisi — konsol 0 hata. **GOTCHA (harness):** `_injectStyle()` yalnız `loadMeclisView()` içinden çağrılıyor — `renderMeclisSalonu()`'yu doğrudan çağıran bir harness CSS'siz kalır (kartlar 2px'e küçülür); `loadMeclisView().catch(()=>{})` bir kez çağrılıp SONRA sabit test verisiyle yeniden çizilmeli.

**Bundle:** _src bundle 852KB gzip (bütçe 650KB) — ÖNCEDEN AÇIK bir risk (bkz [[sistem-saglik-taramasi]] [[kart-uretim-motoru-huzura-cikis]]), bu turda +24% kaynak büyümesi (66KB→82KB, çoğu ikv-primitif reuse ile dengelendi) ama kök neden bu oturumun kapsamı dışında.

**AÇIK RİSK (değişmedi):** ext 11 dilde `prompt.invisible_face` hâlâ eski shadow şeması.

Bkz [[wanderer-gamification-engine]], [[personalization-engine-layers]], [[benlik-karti-2-olunan-ad]] (Olduğun Kişi kutbu), [[olmak-istedigin-kisi]] (Olmak İstediğin Kişi kutbu + oikAddMadde), [[taniyan-ayna-kisiselestirme-3]] (yp/ap birincil seziş kaynağı), [[oruntu-motoru]] (artık dolaylı, yp üzerinden).
