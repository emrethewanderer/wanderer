---
name: tr-en-i18n-tamamlama
description: "TAMAMLANDI (2026-07-03) — gömülü-TR feature modüllerini t()'ye taşıyıp TR+EN tamamlama sprinti; 8 dalga + içerik dalgası; dict 2606 parite. Dalga 9 (2026-07-24): data-i18n-aria erişilebilirlik katmanı + JS-yönetimli eleman gotcha'sı"
metadata: 
  node_type: memory
  type: project
  originSessionId: 161b61c5-2113-4d0f-8e11-e2e9787ad1af
  modified: 2026-07-24T19:45:45.605Z
---

2026-06-27 başladı. Hedef: **TR ve EN %100** (İngilizce kullanıcı hiç Türkçe görmesin). 11 dil SONRA, EN'den üretilecek. Kapsam: UI/chrome modülleri; kart destesi (12b) + arketipler (12a) İÇERİK olarak HARİÇ (sonra). Bkz denetim: [[i18n-bundle-bolme]].

**Kurulan kurallar (kalıp):**
- UI string → `import { t } from './15-i18n.js'` + `t('modprefix.key')`. Anahtarlar `js/parts/15b-i18n-dict-core.js` içinde `tr` VE `en` bloklarına (ikisi paralel, parite şart).
- Değişken: `t('key').replace('{n}', val)` (dil-güvenli kelime sırası; t() interpolasyon yapmaz, sadece p() yapar).
- Modül-yükünde DONMASIN: const dizilerde t() çağırma; çözücüleri fonksiyon yap (örn 02c `catLabel=k=>t('sc.label.'+k)`), render anında çağır.
- İç LLM prompt/context iskeleti: Türkçe BIRAK ama dil-KİLİDİNİ kaldır ("KESİNLİKLE TÜRKÇE yaz/İngilizce kullanma" → "kullanıcının diliyle"). KRİTİK: `getLangInstruction()` (15-i18n) zaten her callLLM'e "[LANGUAGE]: Respond in X" ekliyor (04:29); kilit bununla çakışıp EN çıktıyı Türkçeye zorluyordu = bug.
- Yerleşik EN eşlemeleri: Söz→Promise, Seri→Streak/Day Streak, Günü mühürle→"Seal the day", Geçiş Alanı→"Transition Gateway", zincir→chain, Olduğun Kişi→"Who You Are".
- Doğrulama kapısı: `node -e import dict` parite (tr==en) → `./build.sh|tail` yeşil → preview konsol temiz. Onboarding overlay'leri state-kapılı, preview'da görsel sweep zor → Final dalgaya bırakıldı.

**EN ad eşlemeleri (tutarlı kullan):** Benim Kartım→My Card, Kişilerin Kişileri→People's People, Atölye→Workshop, Geçiş→Transition, Elmas→Diamonds, Söz→Promise, GÖRDÜN/YÜRÜDÜN/OLDUM→YOU SAW/YOU WALKED/YOU BECAME, OLDUĞUN/OLMAN GEREKEN→WHO YOU ARE/WHO YOU MUST BE, Öz Sevgi/Saygı/Değer/Güven/Bolluk→Self-Love/Respect/Worth/Confidence/Abundance.

**YENİ (2026-06-28 Dalga 4'le): statik HTML de kapsama girdi.** Önceki dalgalar (02c/02d, 10A, 10s, 10t/u/f/g/r) tamamen JS-render'dı; Dalga 4 ilk kez statik `_src.html` görünümleri olan modüllerdi (gecis/konusma/degerlendirme/dinlenme/hayalseans/kisilerim/arketipler). Bunların statik chrome'una `data-i18n`/`data-i18n-ph` eklendi (applyTranslations boot'ta gizli view'leri de çevirir). JS-yönetilen dinamik elemanlara data-i18n EKLENMEDİ (clobber önlemi); JS zaten t() ile basıyor. ~~aria-label'lar çevrilmedi (tüm app'te öyle; applyTranslations aria desteklemiyor).~~ **← ARTIK GEÇERSİZ, bkz. aşağıdaki 2026-07-24 kapanışı.**

**Dalga durumu (TaskList #1-8):** Dict şu an **2088/2088 parite**, build yeşil, konsol temiz, 402 vitest geçiyor; preview'da TR+EN canlı render doğrulandı (data-i18n round-trip tüm anahtarları iki dilde çözüyor + JS builder yolları). ⚠ preview testinde dil değişimi reload gerektirir (tarayıcı bundle'ı bellekte tutar; üretimde setLanguage zaten reload eder).
- ✅ Dalga 1: 02d (esik.* 28), 02c (sc.* 93).
- ✅ Dalga 2: 10A-an-karti (ak.* 134; t-shadow `const t=e.target`→`tgt` rename ettim; GEZGİN rumuz markası TR bırakıldı), 10s-gunluk-ritus (gl.* 104; içerik bankaları GL_SOZLER/GL_GIFTS → _sozBank/_giftBank builder fonksiyonlarına çevrildi, _locale() helper toLocaleX için).
- ✅ Dalga 3 (2026-06-28 bitti, 909→1263): 10t (sm.* 53), 10u (us.* 94), 10f (yol.* 67), 10g (wg.* 55), 10r (cz.* 85). Kararlar: **10u seri kartları 10t ile AYNI → sm.card.* paylaşıldı** (hayal/söz ayrı us.hcard/us.zcard); 10f'de yine t-shadow vardı (`const t=TODO[s.id]` → kaldırıldı, ACT map + `t('yol.todo.'+id)`); CAZIBE/SERIES/SM_CARDS gibi modül-const içerik bankaları builder fonksiyonlara çevrildi (_czThoughts/_czPledges/_czTribe/_czPusula, _series(id), _resolveCard); **CAZIBE export'u kaldırıldı** (dışarıdan kullanan yok). 10g admin "Duyuru" sekmesi de çevrildi; ama `_SOZ_THEMES`/`_SOZ_STOP` Emre'nin TR kitaplarında eşleşen **içerik-arama anahtarları → TR bırakıldı** (iç-mantık). _ELMAS_LABEL → `_elmasLabel(reason)` builder. Yüzde işareti dil-farkı anahtara gömüldü (TR `%{pct}` / EN `{pct}%`).
- ✅ Dalga 4 (2026-06-28 bitti, 1263→1640): 10l (rv.* 46), 10k (sk.* 46), 10j (ga.* 72), 10n (dn.* 65), 10i (ha.* 59), 10q (kk.* 89). Kararlar: modül-const içerik bankaları builder'a çevrildi (REVIEW_PERIODS→`_period(p)`+REVIEW_DEFS; DIALOG_SETS→`_dialogSet(k)`+DIALOG_DEFS; CRYSTAL_TIERS→at+glyph kalan dizi + `_crystalName/_crystalDesc(idx)`; HAYAL_KAVRAMLAR→key/category/glyph kalan dizi + `_kavram(key)`; IMPACT_LABELS→`_impactLabel(n)`; OB_STATIONS→OB_STATION_COUNT+`_obStation(i)`; 10q DIM_LABEL→`_dimLabel(d)`, HINT→HINT_KEYS Set+`kkHint`). **Yüzde helper'ı `_pct(n)`=TR `%n`/EN `n%`** (10q'da onlarca `%${x}` yeri); ayrıca pct yüzde anahtarlara gömüldü (`{pct}% complete`). **10n tarih lokalizasyonu**: _AYLAR/_GUNLER dizileri kaldırıldı → `Intl.DateTimeFormat(S._currentLang,…)`; 10i'de toLocaleDateString('tr-TR') → S._currentLang. `t`-gölge düzeltmeleri: `forEach(t=>` → `trk`/`it`; 10n `const t=_todayISO()` → `today`. İç LLM prompt'larda dil kilidi yumuşatıldı (4 modül: "Türkçe"→"kullanıcının diliyle/dilinde"). HAYAL_KAVRAMLAR export'u KORUNDU (10i testi import ediyor). **10i testi düzeltildi**: kısmi `04` mock'u importOriginal'a çevrildi (15-i18n→03-auth-shell→11 zinciri renderHistory istiyor), beforeEach'te `S._currentLang='tr'` pinlendi, label/seed iddiaları i18n sözlüğü kontrolüne taşındı. 12a/12b İÇERİĞİ (kart adı/ders/portre/kategori-etiketi/nadirlik/EMRE_ONERI) bilinçli TR bırakıldı — kapsam dışı.
- ✅ Dalga 5 (2026-06-28 bitti, 1640→1822, +182): 13i (mt.* 23), 13j (wr.* 42), 10v (mr.* 45), 13d (mk.* 60), 10C (sf.* 28). Kararlar: **Emre onayıyla 10v MANIFESTO_12 kitap içeriği ÇEVRİLDİ** (12 madde ad/başlık/özet; tez verbatim "Mesele Sensin"→"It's About You"; XII İslami register korundu: Faith/"Allah Is With People"/tawakkul). MANIFESTO_12 export const → `MANIFESTO_META` (roman+sigil iskelet) + `manifesto12()` builder (mr.item.N.*); `window.MANIFESTO_12` artık **getter** (`Object.defineProperty get: manifesto12`) — 10-features-w2.js:411 "12 Mühür" ızgarası tüketiciyi değiştirmeden dil-duyarlı dizi alır. **REUSE**: 13j SM_NAMES kaldırıldı→`_smName(day)`=`t('sm.card.N.name')`; 10C kategori rozetleri `sc.cat.*.badge`, kicker `ak.share_card_kicker`, ad `ak.my_card_name`; ortak login/loading `ak.toast_login_first`/`wg.admin.loading`. Her modülde `_pct(n)` (TR%n/EN n%); ay-adı dizileri (13j AYLAR) kaldırıldı→`Intl.DateTimeFormat(S._currentLang,{month:'long'})`. İçerik bankaları builder'a: 13i SPEECH→`_speechBank()`. TR-only suffix `_ekiAy` korundu (yalnız TR dalında çağrılır). Statik `#sosyal-view` chrome'una data-i18n (sf.back/title_kicker/topbar_mode). 13d admin "Mektup"+"Kullanıcı Mektupları" sekmeleri de çevrildi; mektup MARKASI (EMRE THE WANDERER/Wanderer Movement) EN bırakıldı; textarea placeholder satır-sonu `\n`→`&#10;` (escapeHTML dönüştürmez). ~~aria-label'lar çevrilmedi (app geneli).~~ **← 2026-07-24'te kapandı (aşağı bak).**
- ✅ Dalga 6 (2026-06-29 bitti, 1822→2088, +266; **7/7 modül**): 09c (mem.* 29), 10o (fg.* 31), 10x (bld.* 23), 13m (kt.* 47), 10w (fm.* yeni 41), 13l (im.* 53), **10-features-w2 (w2.* 75)**. Kararlar: 10o FEATURE_REGISTRY title/sub → titleKey/subKey + `_fTitle/_fSub` resolver (video/poster runtime mutate; glyph brand). 10x testi saf-fonksiyon → güvenli; ~~aria-label'lar TR. 13m aria-label'lar TR (kural)~~ (13m aria'ları 2026-07-24'te `kt.aria_*` anahtarlarına taşındı); `_pct` + countdown birim anahtarları (kt.cd.*); WANDERER STUDIO marka. 10w **model adı/sürüm MARKA**; tagline/desc/starters fallback i18n'e: const tagline/desc→'' + `fmTagline()`(export, 10y:225 de kullanır) + `_mDesc` (desc fallback = mevcut `fm.individual/relationship/work.hint` REUSE) + `_defaultStarters(id)`; greeting saat-dilimi + `fm.aria.toggle` (zaten vardı) kullanıldı; admin "Model Stüdyosu" çevrildi. 13l VIRTUE_LABEL→`_virtueLabel(v)`=t('im.virtue.'+v); IM_TAXONOMY.label→`_txLabel(tx)`=t('im.move.'+type, fallback); `imGetContext` İÇ LLM bağlamı TR skeleton BIRAKILDI (yalnız dinamik etiketler resolver); fmtSince `const t=`→`ts` (t-gölge fix); Benlik Kartı→**Self Card**, Olduğun Kişi→**Who You Are** (kanonik). Ortak admin toast `mk.admin.save_failed` reuse. ⚠ preview boot-sırası: fmRenderControls ilk render dilde donabilir (üretimde dil değişimi reload eder, sorun değil). **10-features-w2** (dev büyük ~200 TR): ay/gün adları Intl (`S._currentLang`); selam `fm.greet.*` REUSE; hava/iç-ses/mode → w2.*; TEMELLER_KEYS/DERINLIKLER_DATA const → id-only + `_temelName/_derinlikName/_derinlikDesc` resolver (map param `t`→`tk`/`x` t-gölge fix); 12-Mühür grid `window.MANIFESTO_12` getter'dan (10v) dil-duyarlı gelir; inline fallback ITEMS defansif TR bırakıldı; `_numSuffix` TR-only guard (EN'de ''); ' çünkü ' kitap-içerik ayrıştırması + lesson cause/effect İÇERİK TR; greeting starters/sefer modalı/dünün-özeti sayfası w2.*. mektup/persona MARKA EN.
- ✅ Dalga 7 (2026-07-03 bitti, 2088→2233, +145): 16 modül taranan (10h,10p,09b,09a,13-extras,11,06,08,07,09,03,12,04,05,13b,gdpr). **Kapsam kararı (AskUserQuestion, Emre onayı)**: (1) chrome-only — derin kitap içerik bankaları (10h SEFER_TASKS ~160 günlük challenge + HASIM_BOSSES + 6 Perde/6 Zehir/7 Tuzak panzehir/personSeed; 10p Suret meydan-okuma şablonları) 12a/12b gibi İÇERİK olarak ERTELENDİ — ayrı bir "içerik dalgası" bekliyor; (2) sinyal regex'lerine EN eşlenik EKLENDİ (aşağıda).
  - **Chrome (06/04/05 sohbet çekirdeği)**: chat.*/time.*/closure.*/history.* ~34 yeni anahtar; tarih dizileri (_TR_DAYS/_TR_MONTHS/MONTHS) kaldırıldı→`Intl.DateTimeFormat(S._currentLang)`; özet hata gösterimi ham `e.message`→`error.summary_failed`.
  - **Chrome (03/07/08/09)**: PREMIUM_FEATURES sabiti (eyebrow/title/desc × 9 özellik) TR-fallback kalır, render-anında `t('premium.<key>.*', fallback)` resolver; sub.*/toast.*/track.*/p13n.*/notify.* anahtarları; 09 t-shadow fix (`map((t,idx))`→`trk`). **Admin-only string'ler (07 kota toast'ları, admin sekme başlıkları) bilinçli TR bırakıldı** — Emre-only yüzey, kapsam dışı (hedef "EN kullanıcı TR görmesin").
  - **Chrome (11/12/13b/gdpr + kalıntı)**: 11 özet blok başlıkları (summary.*) + tarih Intl'e; gdpr onay-anahtarı (**SİL**/**SIFIRLA**) dile-duyarlı yapıldı (`gdpr.delete_keyword`/`reset_keyword` + `{kw}` placeholder — literal karşılaştırma da güncellendi, TR/EN farklı kelime kullanır); ck.*/meclis.*/hayattaki_sen.* fallback-only `t()` çağrıları dict'e eklendi (kod değişmedi).
  - **Sinyal regex EN eşlenikleri**: 09b'nin **tamamı zaten çift-dilliydi** (E2-E17 ekstra katmanları önceki oturumda TR+EN yapılmış) — yalnız kitabın MERKEZİ 3 bloğu (`_DEPTH_SIGNALS` 4 kavram, `_FOUNDATION_SIGNALS` 5 kavram, `_TRANSITION_PATTERNS`) saf TR kalmıştı, mixed-array kalıbıyla (bkz `_DOMAIN_SIGNALS` precedent) EN regex eklendi + 7 testli fonksiyonel doğrulama (geçici test, silindi). 09a + 13-extras **zaten tam çift-dilli** (13-dilli ayrı `16c-i18n-detect-dict.js` sözlüğüne `dp('detect.*')` ile bağlanıyor). Gerçek eksik 08-trends-payment.js'te çıktı: `buildImagePrompt` duygu-tonu tespiti (isBreakthrough/isResistance/isVulnerable/isAwareness) TR-only'idi → EN regex eklendi (seans özeti artık kullanıcının dilinde üretiliyor, bu yüzden EN kullanıcı hep nötr görsele düşüyordu).
  - **Kapı**: dict tr==en 2233/2233 parite; build yeşil; 438 vitest geçti (09b +7 yeni sinyal testi dahil, sonra silindi); preview'da dil-değiştir onay diyaloğu + reload sonrası TAM TR render + konsol temiz doğrulandı (production/minified bundle — `S`/`t()` global expose edilmiyor, doğrulama UI-seviyesinde yapıldı).
  - **AS_TONE_MAP (13-extras) + dataset.intensity gibi 'yüksek'/'normal'/'kritik' iç-anahtarlar bilinçli TR bırakıldı** — CSS/severity-tier anahtarı, kullanıcıya görünmüyor.
- ✅ **Dalga 8 (2026-07-03 bitti, 2233→2606, +373). SPRINT TAMAMLANDI — "Emre: Tüm dalgaları bitirelim."** Kapsam: Dalga 1-6'nın atladığı ~30 modül tam taranıp gerçek chrome ayıklandı (çoğu zaten TR-only regex/prompt/comment idi, gerçek iş azdı) + içerik dalgası (10h/10p).
  - **Hızlı chrome**: 00a-infrastructure (çok erken yüklenen leaf-modül — 15-i18n.js hiçbir şey import etmiyor, döngüsel risk yok, `t` güvenle eklendi) toast; 13a-arac-motoru + 13c-gorsel-ekleme fallback-only anahtarlar dict'e.
  - **10b Gamification**: level adı/açıklama (Usta/Yolcu/Başlangıç) → `t('level.N.name/desc')`; sertifika canvas metni + referral paylaşım metni. **10d günün-düşüncesi**: `DAILY_THOUGHTS` dizisi (14 aforizma, zaten `TODO i18n` yorumu vardı) → `daily.thought.0..13` + `_dailyThought(i)` builder (post-auth boot'ta gösteriliyor, kullanıcı-yüzü onaylandı).
  - **13h Akşam Kapanış Töreni** (major ekran, her akşam 21:00-00:00 tetiklenir): GELDİN/GÖRDÜN/YAPTIN badge'leri 10f'in `yol.verb.seri/hayal/soz` anahtarlarından **REUSE** edildi (Dalga 3'ten); modal başlık/gövde/niyet-input/CTA tam çevrildi (`at.*` 24 anahtar).
  - **13g Paylaşım + 10m Engeller**: paylaşım toast'ları (`shr.*`) + "KİTAPLIK"→LIBRARY kicker; 10m Engeller ekranı (6 Perde/6 Zehir/7 Tuzak sekme UI) `eng.*` chrome (içerik ENGELLER kendisi hariç, o content-wave'e ait).
  - **02b Onboarding Ritüeli** (ESKİ ilk-giriş, artık **canlı fallback** — 09-reports-tracks.js:176 `runOnboardingRitual()` emniyet ağı olarak hâlâ bağlı, EN kullanıcı bu yola düşerse tam TR görürdü): DOMAINS içerik-serbestleştirildi (badge/title/goalPrompt/soru/4-şık × 3 alan × 2 soru), FOUND_LABELS **REUSE** `sc.found.*` (02c'den, Dalga 1), PATTERN_LABELS/FALLBACK yeni `onb.pattern.*`/`onb.fallback.*`; tüm ekranlar (intro/mirror/synth) `onb.*` resolver fonksiyonlarıyla (`_domainText/_qText/_optText`) render-anında çözülüyor (modül-yükünde t() çağrılmıyor).
  - **İçerik dalgası (10h + 10p) — Emre onayıyla, MANIFESTO_12 (Dalga 5) presedansıyla**: **10p zaten tam çift-dilliydi** (SURET_SEFER_TASKS/SEFER_TRIAD/KOK_LABEL/_DEPTH_LABEL hepsi `{tr:[...],en:[...]}` + `_seferLang()` — önceki oturumda yapılmış, hiç iş gerekmedi). **10h gerçek iş**: SEFER_TASKS (126 = 6 kalıp × 21 günlük görev) + SEFER_PROMPTS (6, aslında hiç tüketilmiyordu/dead ama yine de çevrildi) + HASIM_BOSSES (6× name/sub) + ENGELLER (19× name/sub/panzehir/personSeed, 6 Perde+6 Zehir+7 Tuzak) = 222 yeni anahtar, TR kaynak Node scripti ile programatik çıkarıldı (transkripsiyon hatası riski sıfırlandı), EN elle kitap-sesiyle (kısa/buyurgan/ikinci-tekil) çevrildi, 222/222 anahtar birebir eşleşme doğrulandı.
    - **Teknik çözüm — consumer kodu SIFIR değişiklikle çalışsın diye GETTER kalıbı**: `HASIM_BOSSES`/`ENGELLER` artık `.map(item => ({...meta, get name(){return t(...)}, get sub(){...}}))` — `.find(b=>b.id===x).name` gibi mevcut tüm çağrı yerleri (10-features-w2.js, 10m-w2-engeller.js) DEĞİŞMEDEN dil-duyarlı oldu. **SEFER_TASKS** için getter/Symbol.toPrimitive denenip **reddedildi** — `JSON.stringify` (SafeStorage/Supabase insert) `Symbol.toPrimitive`'ı çağırmaz, `{}` olarak serileşirdi; onun yerine `resolveSeferTasks(bossId)` fonksiyonu (düz string[21] döner, sefer BAŞLADIĞI anda bir kez çözülüp DB'ye yazılır — AI-üretilmiş challenge'larla aynı "frozen at creation" davranışı, dil sonradan değişse de başlamış sefer donduğu dilde kalır). `*/ ` içeren yorum satırı block-comment'i erken kapatıp build'i patlattı — dikkat: yorumlarda `kelime.*/başka` gibi dizi yazma.
    - 5 fonksiyonel test (TR+EN getter/resolver doğrulama) yazıldı, geçti, silindi.
  - **Final tarama**: 10z-w2-ses.js'de gerçek 4 eksik anahtar bulundu (ses.unsupported/denied/error/tts_unsupported) — hiçbir dalgada değinilmemiş küçük modül, düzeltildi.
  - **Kapı**: dict tr==en **2606/2606** parite (Dalga 7'nin 2233'ünden +373); build yeşil (bir kez auto-build Stop-hook ile eşzamanlı çalışıp geçici rollup hatası verdi, tekil çalıştırmada temiz); 438 vitest geçti; preview'da `setLanguage('en')` direkt çağrısıyla TAM EN render + konsol tertemiz doğrulandı. **PWA service worker gotcha**: reload sonrası dil bazen eski değere dönüyor gibi görünebilir — bu benim i18n işimle ilgisiz, boot-time dil algılama + SW cache etkileşimi (önceden var, dokunulmadı); `setLanguage()` doğrudan çağrıldığında anında + doğru çalışıyor, dict/render zinciri sağlam.
- **Sprint SONU durumu**: dict 2606/2606, TÜM feature modülleri (00-16 serisi) chrome-i18n taranmış durumda; 12a/12b (arketip/kart destesi İÇERİK) bilinçli kapsam dışı kalmaya devam ediyor — istenirse ayrı bir "Dalga 9: kart içeriği" olarak ele alınabilir.

## Dalga 9 — ERİŞİLEBİLİRLİK KATMANI (2026-07-24, kapandı)

Sprint boyunca üç kez "aria-label'lar çevrilmedi (kural)" diye ERTELENEN boşluk kapandı.
Emre'nin "uygulamayı kullanıcı gibi gez" turunda bulundu: görünen metin EN'ken ekran
okuyucu Türkçe duyuyordu (`_src.html`'de 98 aria-label, %0 i18n).

- **Yeni öznitelik**: `data-i18n-aria` — `applyTranslations()` (15-i18n.js) artık
  `[data-i18n-aria]` elemanlarının `aria-label`'ını da yazar. `-ph`/`-html` ile aynı kalıp.
- **93 statik aria bağlandı**; 49 yeni `aria.*` anahtarı (TR 15b + EN 15e paritede).
  **13 anahtar YENİDEN KULLANILDI** — en güzel keşif: Not Defteri etki ölçeği butonları
  zaten `dn.impact.1..5` + `dn.impact_aria` anahtarlarına sahipti; ayrıca `common.back`
  (11 "Geri"), `gs.aria`, `drawer.notebook`, `ui.day_closed_title`, `ui.feedback_title`.
- **KRİTİK GOTCHA — JS-yönetimli elemanlara statik anahtar TAKMA**: kota halkası
  `[data-kt-ring]` · model pili `#cl-model-pill` / `#ic-models-toggle` · gönder butonu
  `#send-btn`. Bunların aria'sını JS `setAttribute` ile canlı yazar (Durdur↔Gönder,
  "Model: X — değiştir"); statik anahtar takılırsa dil değişimi canlı durumu EZER.
  Bunlar kendi modüllerinde `t()` ile çevrilir → 13m'ye `kt.aria_*` (5 anahtar),
  13d'ye `mk.write_aria`, 09c'ye `aria.emre_memory` eklendi.
- **Kendi açtığım regresyonu öz-denetimde yakaladım**: `#chat-input`'a `data-i18n-ph`
  eklemek Model Stüdyosu'nun özel placeholder cümlesini dil değişiminde eziyordu
  (kimse `i18nchange`'de composer'ı yeniden çizmiyordu). Çözüm: 10y'ye `i18nchange` +
  `i18ndictloaded` dinleyicisi → `llmRenderHome()`. **Sıralama kuralı:** setLanguage
  önce `applyTranslations()` sonra event atar; sidecar geç gelirse `ensureLangDict`
  ikinci kez `applyTranslations()` + `i18ndictloaded` atar — dinamik yazıcılar HER İKİ
  olayı da dinlemeli.
- **Yan bulgu**: 5 kullanıcı-yüzü placeholder i18n'siz kalmış (`nb.compose_ph`,
  `nb.search_ph`, `gl.kanit_ph`, `ui.feedback_ph`, `ch.search_ph`) → bağlandı.
  Admin paneli placeholder'ları (p-name/k-title/push-bc-*/API) bilinçli kapsam dışı.
  `#bugun-greet-input` de JS-yönetimli (10A `ak.greet_ph_*`) → dokunulmadı.
- **Gerçek yazım hatası**: ic modal placeholder `"ne görünüdü?"` (anlamsız kelime) →
  mevcut `fm.input.placeholder` anahtarına bağlandı; `icOpen` artık ön yüz composer'ının
  placeholder'ını AYNALAR (iki yüzde tek davet cümlesi).
- **Kalıcı kapı**: `tests/15-i18n-aria.test.js` (7 test) — davranış + `_src.html`'deki her
  `-aria`/`-ph` anahtarının TR&EN'de varlığı + JS-yönetimli elemanlara anahtar
  takılmadığı. Yeni aria eklerken bu test yolu gösterir.
- Kapı: build yeşil (643KB/650KB), **1136 vitest**, preview'da TR↔EN canlı doğrulandı,
  konsol temiz. DE (`js/parts/i18n/de-*`) yayında değil (`I18N_LANGS` tr/en) — yeni
  `aria.*` anahtarları DE'ye eklenmedi; DE dalga kapısından geçerken eklenmeli.

Bul: `grep -lE "[çğşıöü]" js/parts/*.js` + yorum/iç-prompt hariç tut. Fallback-only `t(key,'TR metin')` çağrıları da EN bug'ı — dict'te anahtar var mı kontrol et. Dinamik anahtar birleştirme (`t('prefix.'+k)`) missing-key script'lerinde YANLIŞ ALARM üretir — gerçek sorun mu diye elle doğrula.
