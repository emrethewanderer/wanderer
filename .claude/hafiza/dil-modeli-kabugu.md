---
name: dil-modeli-kabugu
description: "10y modülü — dil modeli kabuğu: ana kart flip (Sohbet ön yüz ↔ uygulama arka yüz), claude.ai tarzı ana ekran (llm-home), boot artık chat'e iner; ritüel kartı varsayılanı kapalı"
metadata: 
  node_type: memory
  type: project
  originSessionId: c91a8a34-a0fe-468d-8b64-3709c4718575
---

**Dil modeli kabuğu** (10y-w2-llm-shell.js + css/parts/llm-shell.css) — 2026-06-10:
Wanderer "KİŞİSEL DÖNÜŞÜM dil modeli" kimliğine geçti. Uygulama tek KART:
**ön yüz = Sohbet** (girişte ilk ekran), **arka yüz = geri kalan her şey** (Bugün, Atlas, …).
Modeller için bkz [[odak-modelleri]].

- **Flip motoru:** `switchViewHooks.before` kancası — chat ↔ diğer view sınırı aşılınca geçiş iptal edilip iki aşamalı rotateY animasyonu oynar (0→90° kaybol, 90°'de gerçek switchView, -90→0° belir). DOM taşınmaz; durağan hâlde transform YOK (position:fixed bozulmaz). `_flipArmed` ilk boot geçişini animasyonsuz bırakır; `wsFlipTo(v)` dışa açık tetikleyici. prefers-reduced-motion'da düz geçiş.
- **Giriş noktaları:** chat topbar'da `.ch-topbar-flip-btn` → wsFlipTo('bugun'); arka yüzde global `#flip-fab` (✦, sağ alt; after-kancası v!=='chat' iken gösterir) → wsFlipTo('chat'). Drawer/EventBus üzerinden gelen tüm switchView çağrıları da kancayla otomatik flip alır.
- **Ana ekran (llm-home):** bugünün seansı boşsa (`S.chatHistory`'de user/assistant yok) #chat-view `llm-home` sınıfı alır → CSS messages-area+hero'yu gizler, composer'ı merkeze alır, `#llm-home-head` (✦ + selam + "Model · eksen") ve `#llm-starters` çipleri görünür. `llmSyncHome` messages-area MutationObserver'ı + switchView after-kancasıyla eşitlenir; ilk mesajla composer alta kilitlenir.
- **2026-07-03 selam/model-cümlesi ayrımı (iki adımda düzeltildi):** mig 028 model.greeting alanlarını doldurunca `#llm-greeting` saat-bazlı sabit selamın ("İyi geceler, Emre.") YERİNE modelin özel cümlesini basıyordu. Fix adım 1: `fmGreetingText` (10w) artık HER ZAMAN saat-bazlı; adım 2 (Emre'nin ikinci isteğiyle) — model cümlesi ayrı bir satır yerine doğrudan composer'ın **placeholder'ına** taşındı: `fmInputPlaceholder(userName)` (10w) modelin `greeting`'i varsa onu, yoksa varsayılan `"Wanderer'a yaz…"`u döner; 10y `llmRenderHome()` bunu `#chat-input.placeholder`'a yazar. Placeholder metni tarayıcıda `scrollHeight`'e yansımadığından (yalnız gerçek `value` ölçülür) uzun cümleler tek satıra kırpılmasın diye kutu elle ölçülür: `value`'ya placeholder anlık yazılır → `scrollHeight` okunur → `style.height`'e uygulanır → `value` boşaltılır (kullanıcı hiç görmez; `inputEl.value` doluysa bu adım atlanır, autoResize kullanıcı girdisini yönetir). Admin Model Stüdyosu alan etiketi: "Yazı Kutusu Cümlesi". `#llm-greeting-model` / `.llm-home-model-line` (adım 1'de eklenen ara element) SİLİNDİ — artık yok.
- **Composer:** `.cl-composer` (claude.ai tarzı kart) — #chat-input + altta `#cl-model-pill` (fmOpenPicker) + altın daire #send-btn. Eski breath-pill + .input-row HTML'den SİLİNDİ (13-extras pill bloğu null-guard ile no-op). Ritüel kartı (ic-overlay) duruyor ama `_icRitualMode` varsayılanı artık KAPALI (`==='1'` opt-in) — yanıtlar sohbette akar.
- **Boot:** 03-auth-shell initApp varsayılan landing `switchView('chat')` (eski 'bugun'). KRİTİK: landing'den önce bugünün seansı hidre edilir — `S.currentSessId = w3GetDaySessionId(new Date())` + `S.chatHistory = S.allSessions[sid]` kopyası (newSession çağrılmadığı için; yoksa gün içi dönüşte konuşma "boş" sanılır). Opener üretimi hâlâ sadece newSession'da.
- window expose: wsFlipTo, llmStarterSend, llmRenderHome, llmSyncHome. 10w → window.llmRenderHome?.() çağırır (TDZ-güvenli optional).
- **LLM kenar çubuğu (2026-06-10):** Sohbet'te profil cameo'su (`.w2-back-btn`) artık `chDrawerOpen()` — ana menü (w2-drawer) DEĞİL; `#ch-drawer` SOLDAN kayar (eski sağ kayma terk edildi), sağ üstteki saat ikonu KALDIRILDI. Panel = claude.ai sidebar: "✦ BUGÜNÜN SOHBETİ" (chDrawerGoToday) + GEÇMİŞ GÜNLER listesi + altta profil satırı (avatar/ad/aktif model; chDrawerProfile → 180ms sonra navigate settings, flip kancası animasyonu üstlenir; satır detay panelinde `#ch-detail-panel:not(.hidden) ~ .ch-drawer-profile` ile gizlenir). Sol kenar swipe (03-auth-shell) chat'te artık `window.chDrawerOpen?.()`; Escape ch-drawer'ı da kapatır. Ana menüye erişim yalnız arka yüzden. Profil satırı senkronu `_chSyncProfileRow` (11-w2-chat-cal; fmGetActive import'u 10w'den — döngü yok).
- **2026-06-10 topbar revizyonu:** (1) ~~Bugün ws-topbar'ına da kart-çevirme ikonu eklendi~~ → AYNI GÜN GERİ ALINDI: gl-elmas-bar (right:58px absolute) ile çakışıyordu; Bugün topbar'ında flip tuşu YOK, arka yüzden dönüş tek nokta: mini kart FAB. (2) İç Ses/İçsel Durum erişimi chat topbar'ından KALDIRILDI → ch-drawer başlığında "SOHBETLER"in sağında büyük insan-silüeti butonu (`.ch-drawer-icses-btn`, Beden Taraması body-map path'i, `chDrawerClose();wsIcSesAc()`). (3) Gece Nöbeti ayrı `night-watch-bar` şeridi yerine mod rozetinde (`#mode-badge`) 4 sn döngüsüyle "Gece Nöbeti" ↔ mod adı (`startNightWatchBadgeCycle`, 05-closure-parts; onModeBadgeUpdate listener taban metni tazeler; 05:00'te döngü kendini kapatır).
- **2026-06-10 kabuk revizyonu 2 (akşam):**
  - **✦ mini kart FAB:** `#flip-fab` daire değil artık 56×76 mini tarot kartı (`.ff-scene>.ff-card>.ff-face` ön: ✦+SOHBET, arka: desenli sırt); 9 sn'de bir kendi ekseninde tam tur (`ffIdleSpin`, %72'ye kadar durağan) + süzülme. Görünürlük `llmSyncFab()`: switchView after-kancası + auth-screen MutationObserver (oturum-restore yolunda hiç switchView çalışmazsa FAB'sız kalınmasın) + attach'te ilk çağrı; auth görünürken asla.
  - **Composer rejimi:** `#chat-view:not(.llm-home) .cl-composer{display:none}` — claude composer YALNIZ ana ekranda; konuşma akışında eski **atmosfer çizgisi geri geldi** (`#breath-pill` markup'ı yeniden eklendi; `#chat-view.llm-home #breath-pill{display:none!important}` guard) → tık = Ritüel Kartı `icOpen()` (son Emre sözü kartta). 13-extras pill bloğu input-row bağımlılığından arındırıldı. Pill margin'i llm-shell'de override (input-area zaten safe-b padding'li). `_icRitualMode` hâlâ opt-in.
  - **İlk giriş home korunur:** `_homeDismissed` oturum bayrağı — gün DOLU olsa da boot'ta llm-home açılır; composer altında `#llm-continue.show` ("✦ Bugünkü mesajlaşmaya devam et", `llmContinueToday()` → akışa in + reflow zorla + `scrollTo({behavior:'instant'})` — rAF arka plan sekmesinde çalışmaz, smooth görünürlük geçişinde iptal olup 0'da kalır). Dismiss tetikleri: continue tıkı, #send-btn click (capture), chat-input Enter (capture), llmStarterSend. Gün sıfırlanınca (`_todayIsEmpty`) home otomatik döner.
  - **Hidrasyon sağlamlaştı (03-auth-shell):** `allSessions[day_…]` anahtarı tutmazsa (legacy/uuid session_id) bugünün mesajları created_at yerel-gün süzgeciyle bulunur (newSession kalıbı); hidrasyon sonrası `window.llmSyncHome?.()` çağrılır — hidrasyon DOM'a dokunmazsa 10y observer'ı tetiklenmiyordu, devam satırı görünmüyordu.
  - window expose'a `llmContinueToday` eklendi.
- **2026-06-11 ana ekran revizyonu:**
  - **Topbar:** `#us-ring` KALDIRILDI ([[ultra-seri-uc-muhur]]); profil cameo'su 36×48'e büyüdü ve **HEP koçun fotoğrafı** (`COACH_IMG`, config.js — "Emre ile konuşuyorsun" hissi; _src.html'de src hardcoded + w2RefreshProfilePanel her chat geçişinde yazar). Kullanıcının kendi avatarı (S.USER_IMG) profil panelinde + ch-drawer profil satırında (`_chSyncProfileRow` artık topbar'dan kopyalamaz).
  - **Devam satırı taşındı:** `#llm-continue` artık composer altında DEĞİL — `#llm-home-head` içinde, "Model · eksen" alt satırının (llm-home-sub) hemen altında ve onun tasarım dilinde (Cinzel 10px, ls 2.5px, uppercase, altın, çerçevesiz; hover'da glow + ok kayması).
  - **Başlatıcılar:** composer altında kaldı; çipler yenilendi — sol ✦ glyph + sağda hover'da beliren ↗ ok (`.llm-starter-glyph/-text/-arrow`, 10y llmRenderHome markup'ı), nth-child kademeli giriş animasyonu.
- **Tam ekran flip başlığı (2026-06-10):** `#ws-flip-title` (_src.html, flip-fab'ın hemen altı) — kart dönerken yüzün adı ekranı kaplar: arkaya dönüşte **"Wanderer Studio"** (altın gradyan + sub "Hayatını oluşturduğun alan!"), öne dönüşte **"Wanderer"** (lapis gradyan + sub "LLM"). 10y `_showFlipTitle(toBack)` `_flip` içinden tetikler; CSS §1b (llm-shell.css) tek keyframes döngüsü (1.15s, kart 820ms'de biter, başlık ~330ms süzülür), JS sadece `.show` takıp 1200ms'de söker. Perde radial rgba(13,10,7,.66→.94) + backdrop-blur 6px (merkez şeffaf kalırsa arka içerik sub ile çakışıyor — koyulaştırıldı); z-index 11000; pointer-events none; prefers-reduced-motion'da display:none (JS yolu da raw-switch). Marka ayrımı için bkz [[wanderer-studio-marka]].
