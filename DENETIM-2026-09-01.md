# Wanderer Genel Denetimi — "kapısı olmayan kural tavsiyeye döner"

**Tarih:** 2026-09-01 · **Kapsam:** repo bütünü — 85.510 satır JS (124 modül),
22.972 satır CSS, 41.460 satır test, 13 edge function, 11 migration, native
kabuklar, build zinciri. **Yöntem:** kapıların hepsi koşturuldu, uygulama
tarayıcıda boot ettirildi, iddialar koda karşı doğrulandı.

**Tek cümlelik hüküm:** Wanderer olağanüstü disiplinli bir repo — 3.608 test
yeşil, altı denetçi gerçekten çalışıyor, sunucu tarafı örnek düzeyde. Ama
**kapıların yarısı fiilen boş koşuyor** ve **build bu makinede hiç
çalışmıyor**; yani kalite mimarisi Emre'nin dizüstünün dışına çıkamıyor.

---

## Önce hakkını teslim edelim — zaten olanlar

Bunlar süs değil, ölçülmüş gerçekler:

1. **Test kütlesi gerçek.** 153 dosya / 3.608 test, tamamı yeşil (152 sn).
   Tek bir `.skip`, `.only` ya da `.todo` yok. Test kodu (41K satır) üretim
   kodunun yarısı kadar.
2. **Altı denetçi gerçekten kapı.** `gerceklik`, `tasarim`, `bagsiz-ad`,
   `yetim-kopru`, `eksen`, `ihtimalsel` — hepsi koşturuldu, hepsi temiz. Daha
   önemlisi: her biri **kendi kapısını da sınıyor** (`tests/*-kapisi.test.js`
   ikinci describe bloğu uydurma bir ihlal yazıp denetçinin onu yakaladığını
   kanıtlıyor). Yakalamayan kapıyı kapı saymama disiplini nadirdir.
3. **Sunucu tarafı örnek düzeyde.** 38 tablodan 36'sında RLS + policy; admin
   yetkisi `EXISTS (SELECT 1 FROM profiles … is_admin = true)` ile sunucuda
   kuruluyor (`migrations/000_wanderer_schema.sql:306`). Webhook'lar imzalı:
   Svix HMAC-SHA256 (`eposta-sekme/index.ts:63`), HMAC'li unsubscribe linki
   (`bulten-cikis/index.ts:133`), RevenueCat paylaşılan sırrı
   (`revenuecat-webhook/index.ts:84`). `llm-chat` hem token doğruluyor hem
   ücretsiz kademe tavanını 429 ile kesiyor (`llm-chat/index.ts:135`).
4. **CSP tanımlı ve düşünülmüş.** `_src.html:8` — üstelik `frame-ancestors`ın
   `<meta>` ile teslim edildiğinde tarayıcılarca yok sayıldığı bilinip yorumla
   açıklanmış. "Koruyormuş gibi duran" bir satırı silmek, eklemekten zordur.
5. **i18n paritesi kusursuz:** TR 3.520 anahtar, EN 3.520 anahtar, iki yönde de
   sıfır boşluk (ölçüldü).
6. **XSS disiplini gerçek:** 615 `escapeHTML(` çağrısı, DOMPurify katmanı,
   `nosniff` + `Referrer-Policy`.
7. **Teknik borç neredeyse yok:** 85K satırda **3** TODO, **0** `console.log`.
8. **Yorumlar NEDEN yazıyor.** `vite.config.js`'teki `maxWorkers=3` kararı
   tahmin değil, ölçüm: "forks 83.01 sn → threads 48.12 → threads+3 worker
   44.24". Bu repoda yorum, kararın kanıtını taşıyor.
9. **Uygulama sağlam boot ediyor.** Chromium'da açıldı: **0 sayfa hatası**,
   102 `window.*` sözleşmesi ayakta, i18n hazır, preview `no-store` sözünü
   tutuyor, `/sw.js` kill-switch doğru davranıyor.

---

## Boşluklar — 5 grup

### A. Kırık — build bu makinede hiç çalışmıyor 🔴

**A1. `sed -i ''` üç yerde: build Linux'ta exit 2 ile ölüyor.**
`build.sh:50`, `build.sh:57`, `build.sh:83` BSD/macOS sözdizimi kullanıyor:

```bash
sed -i '' 's/<script type="module" crossorigin/<script/' "$TMP/index.html"
```

GNU sed'de `-i` sonrası gelen `''` **boş bir dosya adı** sayılır:
`sed: can't read s/…/: No such file or directory` → `set -euo pipefail`
scripti **exit 2** ile öldürür. Vite derlemesi tamamlanıyor (6.36 sn, 247
modül), sonra atomik takas hiç yapılmıyor. Emre'nin macOS'unda sorun
görünmez; Linux'ta, konteynerde, herhangi bir CI'da build **imkânsız**.
Taşınabilir düzeltme: `sed -i.bak … && rm -f …bak` ya da `perl -pi -e`.

**A2. Hiçbir `.sh` dosyasında execute biti yok.** `git ls-files -s` beşini de
`100644` gösteriyor; protokolün her sayfada tekrarladığı `./build.sh` komutu
`Permission denied` verir — ancak `bash build.sh` çalışır.

### B. Kapı var, tetikleyici yok — kalite mimarisi elle koşuyor 🔴

Repo'nun en güçlü yanı kapı kültürü; en zayıf yanı bu kapıların **hiçbirinin
kendiliğinden koşmaması**.

**B1. Otomasyon yok.** `.github/` dizini yok — workflow, CI, PR kapısı
yok. 3.608 testin ve altı denetçinin tetiği tek bir yerde: geliştiricinin
elle yazdığı `npx vitest run`. `build.sh` de test koşturmuyor (yalnız derleme
+ sidecar + takas).

**B2. `audit-innerhtml.mjs` kapısız — ve şu an kırmızı.** Script
`process.exit(stats.risky > 30 ? 1 : 0)` ile biter; bugün **risky 105**, yani
**exit 1** veriyor. Ama `tests/` içinde onu koşturan tek bir dosya yok,
`build.sh` çağırmıyor, CI yok. Altı denetçinin hepsi `tests/*-kapisi.test.js`
ile bağlıyken bu ailenin tek kapısız üyesi — ve `CODEMOD.md` "CI'da audit
script çalışır + threshold check (şu an aktif)" diye **yanlış** iddia ediyor.

**B3. `npm run typecheck` yapısı gereği hiçbir şey ölçemez.**
`tsconfig.json` `"checkJs": false` taşıyor ve repo'da 2 adet `.d.ts` dışında
TypeScript dosyası yok. Yani komut 85K satır JS'e hiç bakmadan exit 0 verir —
"CI passes" iddiası (`TYPESCRIPT_MIGRATION.md`) doğru ama anlamsız. Gerçek
tablo `tsconfig.strict.json` ile görünür: **3.765 hata** (belge 446 diyor) ve
o komut da `|| true` ile maskeli.

**B4. `check-bundle-size.mjs` kapısız ve bütçesi gevşek.** Ana bundle **2.282
KB ham / 711 KB gzip**; bütçe 3.300 KB / 1.024 KB. Kapı yeşil ama bir PWA
için 711 KB gzip ilk yükleme ağır — ve vite'ın kendi uyarısı ("chunks larger
than 500 kB, consider dynamic import") her build'de basılıyor.

**B5. TR/EN parite kapısı yalnız bir öneki koruyor.** Parite bugün kusursuz
(3520=3520), ama `tests/15-i18n-aria.test.js:79` yalnız `aria.*` önekini
karşılaştırıyor. `tests/i18n-parity.test.js` ise dış diller için yazılmış ve
dürüstçe "kapı boşta bekliyor" diyor. Yani 3.520 anahtarlık paritenin
%97'sini koruyan kapı yok — bugünkü mükemmellik tesadüfe emanet.

**B6. `i18n-validate.mjs --gaps` var olmayan bir dizini arıyor:**
"js/parts/i18n/ altında henüz hiçbir *-ui.js yok". Sözlükler
`js/parts/15b-i18n-dict-core.js` ve `15e-i18n-dict-en.js`'te yaşıyor. Araç
exit 0 veriyor — çünkü bakacak bir şey bulamıyor.

### C. Güvenlik katmanı yarım ve kırılgan 🟠

Sömürülebilir bir XSS **bulamadım** — ama savunma katmanının kendisi hasta.

**C1. Merkezî `escapeHTML` tip-güvensiz — ve bu 26 ikizin sebebi.**
`js/parts/00a-infrastructure.js:791`:

```js
export function escapeHTML(str) {
  if (!str) return '';
  return str.replace(/&/g, '&amp;')…
}
```

Ölçüldü:

| Girdi | `escapeHTML` | modüllerin ikizi |
|---|---|---|
| `123` | **TypeError: str.replace is not a function** | `"123"` |
| `0` | `""` (sayı yok olur) | `"0"` |
| `false` | `""` | `"false"` |
| `{a:1}` | **TypeError** | `"[object Object]"` |

Sonuç: **26 modül kendi `esc`/`_esc` ikizini yazmış**
(`02c-portre.js:81`, `10A-gecis-karti.js:128`, `10q-w2-kisi-karti.js:54`,
`10s-w2-gunluk-ritus.js:39` … hepsi `String(s == null ? '' : s)` ile
başlıyor). Protokolün "tek-kaynak motorlara tüketici ekle, ikiz kurma"
kuralının 26 kez çiğnenmesi bir disiplinsizlik değil — **tek kaynağın kırık
olmasının semptomu.** Testte de kör nokta var:
`tests/00a-infrastructure.test.js:346` yalnız `null`'ı deniyor, sayı vakası
yok. 615 çağrının hiçbirinde bugün sayı geçtiğini kanıtlayamadım; koruma da
yok.

**C2. `safeHTML`/`setHTML` katmanının sıfır tüketicisi var.**
`js/parts/00c-html-safe.js` yazılmış, `js/main.js:21` import ediyor,
`main.js:690` window'a açıyor — ama `grep -rn "setHTML(\|safeHTML(" js/`
(kendi dosyası hariç) **0** döndürüyor. `CODEMOD.md`'nin "Phase B" yol
haritası hiç uygulanmamış. Ölü bir savunma katmanı taşınıyor.

**C3. DOMPurify testte passthrough mock'lu — sanitizasyon hiç sınanmıyor.**
`tests/setup.js:96` global olarak `sanitize: (s) => String(s)` enjekte ediyor.
`tests/00c-html-safe.test.js`'in 11 testi API yüzeyini ölçüyor, korumayı
değil — dosyanın kendi başlığı bunu itiraf ediyor: "Real XSS sanitization is
verified in production via DOMPurify itself." Yani üretimde doğrulandığı
varsayılıyor, hiçbir yerde ölçülmüyor. En az bir testin gerçek DOMPurify ile
gerçek bir vektör denemesi gerek.

**C4. DOMPurify 3.4.4 — 8 açık danışma (high).** `npm audit`: IN_PLACE bypass,
`ALLOWED_ATTR` kalıcı kirlenmesi, `SAFE_FOR_TEMPLATES` bypass ve dördü daha.
Ayrıca `tar` üzerinden 1 **critical** (capacitor/cli zinciri). `npm audit fix`
mevcut.

**C5. `ALLOWED_ATTR` içinde `onclick` ve `onkeydown` var.**
`00c-html-safe.js:24` — bilinçli ve yorumlanmış ("template'ler yaygın olarak
onclick kullanıyor"), ama sanitizer'ın olay-işleyici filtresini büyük ölçüde
etkisiz kılıyor. `safeMarkdownHTML` (LLM çıktısı) doğru davranıp onclick'i
yasaklıyor — asıl korunması gereken yüzey orası, yani risk sınırlı. Yine de
`setHTML` bir gün kullanıcı verisi taşırsa bu kapı açık kalır.

**C6. CSP `script-src 'unsafe-inline'` taşıyor.** `_src.html:10`. Sebebi
mimari: `_src.html`'de **241 inline `onclick`** var. Bu, XSS'e karşı ikinci
savunma hattını (CSP) fiilen devre dışı bırakır — C5 ile birleşince katmanlı
savunma tek katmana iner.

**C7. Sabit-zamanlı karşılaştırma yalnız bir yerde.** `safeEq`
(`bulten-cikis/index.ts:61`) doğru yazılmış, ama iki webhook düz `===` ile
karşılaştırıyor: `revenuecat-webhook/index.ts:84` ve
`eposta-sekme/index.ts:81`. Aynı repoda çözüm var, iki yerde kullanılmamış.

**C8. Clickjacking koruması yalnız bir yorumda yaşıyor.** `_src.html:27`
"hosting/CDN katmanında verilir" diyor — repo'da bunu kanıtlayan hiçbir
yapılandırma yok (`netlify.toml`, `vercel.json`, `_headers` yok). Protokolün
"deploy edilmiş varsayma" kuralı gereği bu **doğrulanmamış** sayılmalı.

### D. Senkron kaymaları — belge ve dağıtım gerçeği söylemiyor 🟠

**D1. Native kabuklar bayat bundle taşıyor.** Web `_src-CUyScgB6.js` (2.337
KB), Android ve iOS ise `_src-cJ7_ajeY.js` (2.620 KB) — **283 KB fark**,
farklı hash. `npx cap sync` çalıştırılmamış. Mağazaya giden uygulama web'den
geride; `android/` ve `ios/` altındaki kopyalar commit'li olduğu için bu fark
sessizce taşınıyor.

**D2. `CODEMOD.md` gerçeği söylemiyor.** İddia: 165 `innerHTML`, 48 risky.
Gerçek: **379 / 105**. Ayrıca "CI'da audit script çalışır" — çalışmıyor (B2).

**D3. `TYPESCRIPT_MIGRATION.md` gerçeği söylemiyor.** İddia: 446 hata,
TS2304 = 100 "gerçek bug". Gerçek: **3.765 hata**, TS2304 = **0** (o iş
bitmiş, belge güncellenmemiş), TS2339 = 3.425.

**D4. `audit-innerhtml.mjs` yanlış soruyu soruyor.** Motor `.innerHTML =`
satırını bulup **±2 satırlık pencereye** bakıyor (`:41`). Bu iki yönde de
yalan söyler: 6 satır aşağıdaki `escapeHTML`'i göremediği için temiz kodu
"risky" sayar; komşu satırdaki alakasız bir `escapeHTML` yüzünden gerçek
riski "escaped" damgalar — ikincisi tehlikelidir. Kendi ifade-bazlı
sayacımı yazıp koşturdum: o da bu repoda doğru sayamıyor, çünkü Wanderer'ın
HTML üretim deseni **"HTML döndüren yardımcı fonksiyonlar"** üzerine kurulu
(`${_atlRing(1)}`, `${kkRenderCard3D(prev, {})}`). Böyle bir kod tabanında
satır ya da ifade bazlı bir sayaç ilkece doğru sonuç veremez — kapı
fonksiyon-döndürür-HTML sözleşmesine bakmalı, `innerHTML` satırına değil.

### E. Ağırlık ve artıklar 🟡

**E1. Ölü küme `12a-archetypes.js`'te duruyor.** `wsArchTraitsHTML:540`,
`_openTraitPopup:559`, `TRAIT_FIELDS:532`, `TRAIT_SCALE:538` — dört tanımın da
dış çağrısı **0**. `CODEMOD.md` bunu biliyor ve "ayrı temizlik turunda
sökülebilir" diye bırakmış; tur hiç gelmemiş. (`10j-w2-gecis-alani.js` ve
`gecis-alani.css` silinmesi ise doğrulandı — orası temiz.)

**E2. Aynı görsel iki kez commit'li.** `emre-portre.png` ve `icon-512.png`
birebir aynı dosya (md5 `2cd296ef…`), her biri 487 KB.

**E3. Çalışma ağacı 36 MB** — `dist/` (5.5M), `android/` (4.9M), `ios/`
(4.7M), `assets/` (4.1M) hepsi commit'li. Türetilmiş çıktıların repo'da
taşınması D1'deki sessiz sürüm kaymasını mümkün kılan şeyin ta kendisi.

**E4. JSDoc'ta üç geçersiz karakter.** `00g-topbar-yildizlari.js:68`,
`05-closure-parts.js:109,111` — `@param {T} [ad] — açıklama` biçimindeki
em-dash TypeScript'in JSDoc çözümleyicisini kırıyor (TS1127). Kozmetik ama
o parametrelerin tip bilgisi düşüyor.

---

## Öncelik sırası

| # | Bulgu | Etki | Emek |
|---|---|---|---|
| 1 | A1 — `sed -i ''` build'i Linux'ta öldürüyor | CI/konteyner imkânsız | 3 satır |
| 2 | C4 — DOMPurify 3.4.4 + tar açıkları | 8 high + 1 critical | `npm audit fix` |
| 3 | C1 — `escapeHTML` tip-güvensiz, 26 ikiz | çökme riski + tek-kaynak ihlali | 1 fonksiyon + göç |
| 4 | D1 — native bayat bundle | mağazaya eski sürüm | `cap sync` |
| 5 | B1 — otomatik kapı yok | tüm kalite mimarisi elle | 1 workflow |
| 6 | C3 — sanitizasyon sınanmıyor | savunma ölçülmemiş | 1 test dosyası |
| 7 | B2/B4 — audit + bundle kapısız | kural tavsiyeye dönüyor | 2 kapı testi |
| 8 | D2/D3 — belgeler bayat | yanlış zemin | belge güncelleme |
| 9 | C2 — `safeHTML` ölü katman | ya kullan ya kaldır | karar |
| 10 | C7, C8, E1, E2, E4 | temizlik | küçük |

---

## Doğrulama — bu denetimde koşturulanlar

| Kontrol | Sonuç |
|---|---|
| `npx vitest run` (tam süit) | ✅ 153 dosya / 3.608 test |
| `gerceklik-denetci.mjs` | ✅ temiz (135 dosya) |
| `tasarim-denetci.mjs` | ✅ temiz (186 dosya) |
| `bagsiz-ad-denetci.mjs` | ✅ temiz |
| `yetim-kopru-denetci.mjs` | ✅ temiz (145 dosya, 1077 expose) |
| `eksen-denetci.mjs` | ✅ taban içinde |
| `ihtimalsel-denetci.mjs` | ✅ temiz |
| `check-bundle-size.mjs` | ✅ bütçe içinde (bütçe gevşek) |
| `audit-innerhtml.mjs` | ❌ **exit 1** (risky 105 > 30) |
| `bash build.sh` | ❌ **exit 2** (sed) |
| `npm run typecheck` | ✅ exit 0 (ama `checkJs:false`) |
| `tsc -p tsconfig.strict.json` | 3.765 hata (baseline) |
| `npm audit --omit=dev` | ❌ 2 high + 1 critical |
| Preview + Chromium boot | ✅ 0 sayfa hatası, 102 sözleşme |
| TR/EN sözlük paritesi | ✅ 3520 = 3520 |

---

*Denetimin özeti tek cümlede: Wanderer'ın kalite mimarisi gerçek ve nadir —
ama kendi kapılarının yarısını çalıştırmıyor, ve kapıları çalıştıracak makine
(build) bu repoyu klonlayan hiç kimsede çalışmıyor. Mesele burada da aynı:
söylenen ile ölçülen arasındaki fark.*

---

# ONARIM TURU — 2026-09-02

Emre'nin kararı: *"Hepsini sıra sıra düzeltelim."* Aşağıdaki tablo her
bulgunun akıbetidir. "Kapı" sütunu, düzeltmenin geri gelmesini engelleyen
bekçiyi gösterir — bu sprintin ölçüsü buydu: **her düzeltme kendi kapısını
bıraksın**, yoksa aynı fark bir sonraki sprintte geri gelir.

| # | Bulgu | Akıbet | Kapı |
|---|---|---|---|
| A1 | `sed -i ''` build'i Linux'ta öldürüyor | ✅ `-i.bak` + `rm -f` (her iki sed'de çalışır) | CI `build` adımı |
| A2 | `.sh` dosyalarında execute biti yok | ✅ beşi de `100755` | — |
| B1 | Otomatik tetikleyici yok | ✅ `.github/workflows/kapi.yml` | kendisi kapı |
| B2 | `audit-innerhtml` kapısız (ve kırmızı) | ✅ motor yenilendi + tabana bağlandı | `tests/xss-kapisi.test.js` |
| B3 | `typecheck` boş koşuyor (`checkJs:false`) | ⚠️ **bilerek bırakıldı** — açmak 3.758 hatayı kapıya sokar; belgeye dürüstçe yazıldı | `TYPESCRIPT_MIGRATION.md` |
| B4 | Bundle bütçesi kapısız | ✅ kapıya bağlandı (bütçe değeri değişmedi) | `tests/bundle-kapisi.test.js` |
| B5 | Parite kapısı yalnız `aria.*` önekini koruyor | ✅ tam sözlük paritesi kapıya bağlandı | `tests/i18n-tam-parite-kapisi.test.js` |
| B6 | `i18n-validate --gaps` "olmayan dizini arıyor" | ↩️ **bulgu geri çekildi** — araç dış diller için yazılmış, dış dil henüz yok; "boşta bekleyen araç", kusur değil | — |
| C1 | `escapeHTML` tip-güvensiz, 22 ikiz | ✅ helper sertleştirildi, bütün ikizler delege ediyor | `00a-infrastructure.test.js` tip bloğu |
| C2 | `safeHTML` katmanının sıfır tüketicisi | ⚠️ **korundu** — katman onarıldı ve testlendi; bağlanması ayrı bir iş (CODEMOD.md) | `00c-html-safe-gercek.test.js` |
| C3 | Sanitizasyon hiç ölçülmüyor (mock) | ✅ gerçek DOMPurify ile 18 test | aynı dosya |
| C4 | DOMPurify 3.4.4 + tar açıkları | ✅ 3.4.14, `npm audit` 0 açık | CI `npm audit` adımı |
| C5 | `ALLOWED_ATTR`'da `onclick` | ✅ **bulgu derinleşti** — çapraz denetim, `ADD_URI_SAFE_ATTR` eksikliği yüzünden `target`/`tabindex`/`data-*`'ın da sessizce silindiğini kanıtladı; config onarıldı, `onclick` allowlist'ten çıkarıldı | 5 yeni config testi |
| C6 | CSP `script-src 'unsafe-inline'` | ⚠️ **bilerek bırakıldı** — 241 inline `onclick`'in kaldırılması mimari bir iş, ayrı sprint | — |
| C7 | `safeEq` iki webhook'ta kullanılmıyor | ✅ ikisi de sabit zamanlı karşılaştırmaya geçti | — |
| C8 | Clickjacking koruması yalnız yorumda | ✅ `public/_headers` + `SETUP-GUVENLIK-BASLIKLARI.md` | ELLE adım belgelendi |
| D1 | Native kabuklar bayat bundle taşıyor | ✅ `cap copy` build'e girdi | `tests/native-senkron-kapisi.test.js` |
| D2 | İki belge gerçeği söylemiyor | ✅ ikisi de ölçülerek yeniden yazıldı | — |
| D3 | Audit motoru yanlış soruyu soruyor | ✅ ifade-bazlı, HTML üreten her template'i tarar | `tests/xss-kapisi.test.js` |
| E1 | `12a-archetypes.js`'te ölü küme | ✅ 9 tanım söküldü (681 → 488 satır) | `bagsiz-ad` + `yetim-kopru` denetçileri |
| E2 | `emre-portre.png` = `icon-512.png` | ⚠️ **bilerek bırakıldı** — aşağıya bkz. | — |
| E3 | Çalışma ağacı 36 MB (türetilmiş çıktı commit'li) | ⚠️ **bilerek bırakıldı** — native senkron kapısı tam da bu kopyaların varlığına dayanıyor | — |
| E4 | JSDoc em-dash (TS1127 ×3) | ✅ ayırıcı `-` yapıldı, TS1127 = 0 | strict typecheck |

## Yapılmayanlar ve gerekçeleri

**E2 — aynı görselin iki adı.** `emre-portre.png` ve `icon-512.png` birebir
aynı 512×512 dosya (487 KB × 2 kaynak + 2 build çıktısı). Birini silip
diğerine yönlendirmek 487 KB kazandırır ama **iki ayrı rolü birbirine
bağlar**: biri Emre'nin portresi (ürünün anlam ekseninde `.wns-portrait`),
diğeri PWA ikonu. PWA ikonu ileride maskable/kare olarak ayrı tasarlanırsa
ayrışmaları gerekir. Asıl kazanç zaten birleştirmek değil, 487 KB'lık PNG'yi
optimize etmektir — bu ortamda `optipng`/`pngquant`/Pillow yok ve yeniden
kodlama görsel bir karardır, Emre'nindir.

**B3 ve C6** teknik borcun kendisidir, kusur değil: `checkJs:true` 3.758
hatayı, inline `onclick`'lerin kaldırılması 241 çağrı noktasını açar. İkisi
de ayrı birer sprint; belgeye dürüstçe yazıldılar ki "kapı var" sanılmasın.

## Onarım sonrası doğrulama

| Kontrol | Önce | Sonra |
|---|---|---|
| `bash build.sh` | ❌ exit 2 | ✅ exit 0 |
| Tam süit | 153 dosya / 3.608 test | ✅ **158 dosya / 3.645 test** |
| `npm audit --omit=dev` | ❌ 2 high + 1 critical | ✅ 0 açık |
| `audit-innerhtml.mjs` | ❌ exit 1, kapısız | ✅ exit 0, kapıya bağlı |
| TS1127 | 3 | ✅ 0 |
| Native bundle senkronu | ❌ 283 KB geride | ✅ aynı hash |
| Otomatik kapı | ❌ yok | ✅ GitHub Actions |
| `12a-archetypes.js` | 681 satır | ✅ 488 satır |
